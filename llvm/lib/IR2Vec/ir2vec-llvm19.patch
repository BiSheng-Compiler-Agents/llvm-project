diff --git a/CMakeLists.txt b/CMakeLists.txt
index bc46bbf..77ba29f 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -10,11 +10,6 @@ set(CMAKE_RUNTIME_OUTPUT_DIRECTORY ${CMAKE_BINARY_DIR}/bin)
 
 find_package (Eigen3 3.3.7 QUIET NO_MODULE)
 
-if (Eigen3_FOUND)
-    message(STATUS "Found Eigen3: ${Eigen3_DIR}")
-    add_definitions(-DEIGEN_FOUND)
-endif()
-
 set(CMAKE_CXX_STANDARD 17 CACHE STRING "")
 
 # LLVM is normally built without RTTI. Be consistent with that.
diff --git a/src/CMakeLists.txt b/src/CMakeLists.txt
index 5cab0cf..1e60778 100644
--- a/src/CMakeLists.txt
+++ b/src/CMakeLists.txt
@@ -2,8 +2,6 @@ configure_file (./include/version.h.cmake version.h @ONLY)
 include_directories(./include ${CMAKE_CURRENT_BINARY_DIR})
 include_directories(${CMAKE_BINARY_DIR})
 
-option(LLVM_IR2VEC "where to enable IR2Vec as subproject for LLVM" OFF)
-
 set(GENERATED_HEADERS_DIR "${CMAKE_BINARY_DIR}/include")
 file(MAKE_DIRECTORY ${GENERATED_HEADERS_DIR})
 
@@ -64,76 +62,21 @@ set(commonsrc FlowAware.cpp Symbolic.cpp utils.cpp ${GENERATED_HEADERS_DIR}/Voca
 set(libsrc libIR2Vec.cpp ${commonsrc})
 set(binsrc CollectIR.cpp IR2Vec.cpp)
 
-if(NOT LLVM_IR2VEC)
-
-  set(LT_LLVM_INSTALL_DIR "" CACHE PATH "LLVM installation directory")
-  list(APPEND CMAKE_PREFIX_PATH "${LT_LLVM_INSTALL_DIR}/lib/cmake/llvm/")
-
-  find_package(LLVM 19.1.7 REQUIRED CONFIG)
-  message(STATUS "Found LLVM ${LLVM_PACKAGE_VERSION}")
-  message(STATUS "Using LLVMConfig.cmake in: ${LLVM_DIR}")
-
-
-
-  include_directories(SYSTEM ${LLVM_INCLUDE_DIRS})
-  # llvm_map_components_to_libnames(llvm_libs all)
-  llvm_map_components_to_libnames(llvm_libs support core irreader analysis TransformUtils)
-
-  add_executable(${PROJECT_NAME} ${binsrc})
-  target_link_libraries (${PROJECT_NAME} ${llvm_libs} objlib)
-  target_include_directories(${PROJECT_NAME} PRIVATE .)
-
-  add_library(objlib OBJECT ${libsrc})
-  set_property(TARGET objlib PROPERTY POSITION_INDEPENDENT_CODE 1)
-  if(Eigen3_FOUND)
-    target_link_libraries (objlib Eigen3::Eigen)
-  endif()
-
-  add_library(${IR2VEC_LIB} SHARED $<TARGET_OBJECTS:objlib>)
-  add_library(${IR2VEC_LIB_STATIC} STATIC $<TARGET_OBJECTS:objlib>)
-
-  set_target_properties(${IR2VEC_LIB} ${IR2VEC_LIB_STATIC} PROPERTIES
-      VERSION ${PROJECT_VERSION}
-      SOVERSION 1
-      PUBLIC_HEADER "./include/IR2Vec.h"
-      PUBLIC_HEADER "${GENERATED_VOCAB_HEADERS}"
-      OUTPUT_NAME ${IR2VEC_LIB}
-      LIBRARY_OUTPUT_DIRECTORY ${CMAKE_BINARY_DIR}/lib
-      ARCHIVE_OUTPUT_DIRECTORY ${CMAKE_BINARY_DIR}/lib
-      )
-
-  install(TARGETS ${IR2VEC_LIB} ${IR2VEC_LIB_STATIC}
-      LIBRARY DESTINATION lib
-      PUBLIC_HEADER DESTINATION include
-      RESOURCE DESTINATION ./)
-
-  add_subdirectory(test-suite)
-
-  add_custom_target(check_ir2vec
-    COMMAND python3 test-lit.py -a .
-    COMMENT "Running LIT based test-suite"
-    WORKING_DIRECTORY ./test-suite
-    DEPENDS ${PROJECT_NAME}
-    VERBATIM
-  )
-
+if(NOT LLVM_VERSION_MAJOR EQUAL 19)
+  message(FATAL_ERROR "Unspoorted LLVM version: expected 19 got ${LLVM_VERSION_MAJOR}")
 else()
+  message(STATUS "Using LLVM version 19")
+endif()
 
-  file(COPY ${CMAKE_CURRENT_SOURCE_DIR}/include/IR2Vec.h DESTINATION ${LLVM_MAIN_INCLUDE_DIR}/llvm )
-
-  set(LLVM_OPTIONAL_SOURCES ${binsrc})
+set(LLVM_OPTIONAL_SOURCES ${binsrc})
 
-  add_llvm_library(LLVMIR2Vec
-    ${libsrc}
+add_llvm_component_library(LLVMIR2Vec
+  ${libsrc}
 
-    DEPENDS
-    intrinsics_gen
-  )
+  DEPENDS
+  intrinsics_gen
+)
 
-  if(Eigen3_FOUND)
-    target_link_libraries(LLVMIR2Vec PRIVATE Eigen3::Eigen)
-  endif()
-  target_include_directories(LLVMIR2Vec PRIVATE ${LLVM_MAIN_INCLUDE_DIR})
-  target_include_directories(LLVMIR2Vec PRIVATE .)
+target_include_directories(LLVMIR2Vec PRIVATE ${LLVM_MAIN_INCLUDE_DIR})
+target_include_directories(LLVMIR2Vec PRIVATE .)
 
-endif()
diff --git a/src/generate_vocabulary.py b/src/generate_vocabulary.py
index 8006871..3181ef1 100644
--- a/src/generate_vocabulary.py
+++ b/src/generate_vocabulary.py
@@ -45,7 +45,7 @@ def generate_base_vocabulary_header():
         "#include <string>\n"
         "#include <vector>\n"
         "#include <memory>\n"
-        "#include <stdexcept> // For std::invalid_argument\n"
+        "#include <cassert>\n"
         "namespace IR2Vec {\n\n"
         "using Vector = std::vector<double>;\n\n"
         "class VocabularyBase {\n"
@@ -139,7 +139,7 @@ def generate_vocabulary_factory(class_names):
 
     factory_footer = """\
         default:
-            throw std::invalid_argument("Invalid dimension for vocabulary creation.");
+            assert("Invalid dimension for vocabulary creation.");
     }
 }
 } // namespace IR2Vec\n
diff --git a/src/include/VectorSolver.h b/src/include/VectorSolver.h
index 6b12c97..51635fc 100644
--- a/src/include/VectorSolver.h
+++ b/src/include/VectorSolver.h
@@ -10,6 +10,7 @@
 #define __VECTOR_SOLVER_H__
 
 #include <algorithm>
+#include <cassert>
 #include <cmath>
 #include <iostream>
 #include <limits>
@@ -70,8 +71,7 @@ matrix solve(matrix &A, matrix &B) {
 
   // Check if dimensions are compatible (m rows in A, same m rows in B)
   if (m != B.size()) {
-    throw std::invalid_argument(
-        "Matrix dimensions are not compatible for solving AX=B");
+        assert("Matrix dimensions are not compatible for solving AX=B");
   }
 
   matrix augmented(m, std::vector<double>(m + n));
